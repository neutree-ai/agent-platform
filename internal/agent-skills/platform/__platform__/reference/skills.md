# Skills

To create or edit a skill, use `skill_create_draft` / `skill_enter_edit` / `skill_publish`. Follow the `skill-creator` skill for SKILL.md conventions (ask the user to install it if missing).
