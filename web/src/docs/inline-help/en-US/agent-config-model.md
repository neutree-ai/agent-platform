## Model

Select the agent runtime type and model.

### Model / Small Model

- **Small Model** is used for lightweight tasks such as completion and summarization; when left empty, the primary model is used automatically
- Switching Agent Type resets Agent Settings to the defaults for that type
