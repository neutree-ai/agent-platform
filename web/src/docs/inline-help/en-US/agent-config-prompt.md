## Prompt

Configure the agent's system prompt to define its role and behavior.

When linked to a Prompt Library entry, the content is automatically synchronized to all associated Workspaces as the prompt version updates.

### Writing tips

- Define the agent's role and scope of responsibilities clearly
- List the available tools and MCP services
- Define output format and language preferences
- Add safety constraints, such as "Do not perform destructive operations"
