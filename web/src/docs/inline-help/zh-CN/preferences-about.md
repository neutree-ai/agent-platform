## 关于

此处显示当前 web 构建的版本信息。

- **当前版本**：浏览器中加载的 bundle 对应的 commit
- **构建时间**：该镜像在 CI 中打包的时间（UTC）
- **服务端版本**：服务器当前提供的最新版本

如果当前版本与服务端版本不同，意味着已部署了新版本。系统会自动检测并提示刷新，您也可以手动点击「刷新」加载最新代码。

升级 / 自部署校验：通过对比这里的版本，可以快速确认本机连接的服务是否已切换到新镜像。
